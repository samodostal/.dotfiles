gui:
  showBottomLine: false
git:
  paging:
    colorArg: always
    pager: delta --dark --paging=never
os:
  editCommand: 'nvim'
  # editCommandTemplate: '{{editor}} --server /tmp/nvim-server-$(dirname.sh).pipe --remote-tab "$(pwd)/{{filename}}"'
